name: CI

on:
  push:
    branches: hw1

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run a one-line script
        run: sh ./script_install.sh
